# Hyperledger Fabric Network Dependencies
# These are for Python-based Fabric client applications

# Core Fabric SDK
fabric-sdk-py>=1.0.0 # Keep this for now, issue likely with its dependencies

# gRPC and Protocol Buffers (Adjusted for Python 3.9.x compatibility)
grpcio==1.49.1 # Confirmed good for Python 3.9 by many sources
grpcio-tools==1.49.1 # Match grpcio version
protobuf==3.20.3 # Crucial for grpcio compatibility with Python 3.9

# Cryptographic Libraries (Adjusted for Python 3.9.x compatibility)
cryptography==3.4.8 # Good for Python 3.9
ecdsa>=0.18.0 # Likely fine
secp256k1==0.14.0 # Pin exact if issues persist, check its docs for 3.9
coincurve==18.0.0 # Pin exact if issues persist, check its docs for 3.9

# Networking and HTTP
requests>=2.20.0
aiohttp>=3.9.0

# Configuration and Environment
python-dotenv>=1.0.1
pyyaml>=6.0.1

# Logging and Monitoring
structlog>=23.2.0
prometheus-client>=0.19.0

# Development and Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Utilities
click>=8.1.0
typer>=0.9.0
rich>=13.7.0

# Data Processing (Adjusted for Python 3.9.x compatibility)
pandas==1.5.3 # Good for Python 3.9
numpy==1.24.4 # Good for Python 3.9
pysha3==1.0.0 # Keep for now, but watch if build fails here

# Security
passlib>=1.7.4
bcrypt>=4.1.0
