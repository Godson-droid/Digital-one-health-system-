# Hyperledger Fabric Network Dependencies
# These are for Python-based Fabric client applications

# Core Fabric SDK
fabric-sdk-py # Check its compatibility with Python 3.10/3.11 - likely fine

# gRPC and Protocol Buffers (Now compatible with newer Python)
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0 # Or even higher, depending on latest grpcio-tools requirements

# Cryptographic Libraries (Revert to modern versions)
cryptography>=42.0.7
ecdsa>=0.18.0
secp256k1>=0.14.0
coincurve>=18.0.0

# Networking and HTTP
requests>=2.20.0
aiohttp>=3.9.0

# Configuration and Environment
python-dotenv>=1.0.1
pyyaml>=6.0.1

# Logging and Monitoring
structlog>=23.2.0
prometheus-client>=0.19.

# Development and Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Utilities
click>=8.1.0
typer>=0.9.0
rich>=13.7.0

# Web Server
gunicorn==20.1.0
uvicorn>=0.17.0 # Pin to a stable version
fastapi==0.111.0

# Data Processing (Revert to modern versions)
pandas>=2.2.0
numpy>=1.26.0
pysha3 # You can try 1.0.6 again, or just 'pysha3' to get the latest compatible

# Security
passlib>=1.7.4
bcrypt>=4.1.0
